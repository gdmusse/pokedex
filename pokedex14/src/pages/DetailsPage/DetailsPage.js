import { useHistory } from "react-router-dom";


const Detailspage = () => {
    const history = useHistory();

    return (
        <div>
            "DetailsPage"
        </div>
      );
    };

export default Detailspage;
    