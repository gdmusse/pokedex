import { useHistory } from "react-router-dom";


const Pokedexpage = () => {
    const history = useHistory();

    return (
        <div>
            "PokedexPage"
        </div>
      );
    };

export default Pokedexpage;
    